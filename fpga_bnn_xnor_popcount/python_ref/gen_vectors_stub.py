from __future__ import annotations
"""
Later:
- generate random packed vectors
- compute expected acc with bnn_golden.dot_acc
- emit to sim/vectors/ for testbench to load
We keep as stub since local execution isn't available yet.
"""
def main():
    print("Run later: generate packed vectors + expected outputs for randomized regression tests.")

if __name__ == "__main__":
    main()
