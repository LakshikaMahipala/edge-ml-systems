# Experiment plan 

1) Enumerate 2–3 architectures and 3–4 hardware configs
2) Compute:
   - acc_proxy(arch)
   - latency_proxy(arch, hw)
   - energy_proxy(arch, hw)
3) Build Pareto front
4) Select 2–3 representative points:
   - fast point
   - balanced point
   - accurate point
5) Write results table for Mini-project 13
