Pruning 

Key lesson
- unstructured pruning helps storage but not guaranteed speed
- structured pruning changes shapes and can yield real dense speedups
- FPGA prefers structured pruning unless you build a sparse datapath
