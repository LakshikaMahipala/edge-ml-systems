# Honesty statement

Until real FPGA measurements are added:
- this module contains templates + validation code
- no claims are made about real latency numbers

After adding measurements:
- the repo will report real measured values with device + clock recorded
