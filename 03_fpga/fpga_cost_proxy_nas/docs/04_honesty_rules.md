# Honesty rules

- We do not claim real FPGA latency/area from this proxy.
- We call it "proxy cycles / proxy LUT / proxy BRAM".
- Purpose is ranking and design intuition only.
- Real numbers require synthesis + timing + or measured kernel.
