# Evidence policy (systems/scheduling)

Rules:
- Any bandwidth or latency values are labeled "proxy" unless measured.
- Schedules are only meaningful relative to the latency registry that generated them.
- HEFT vs OPT comparison is valid for small DAGs (bruteforce OPT).
- Hardware conclusions require later calibration with real FPGA kernel points.
