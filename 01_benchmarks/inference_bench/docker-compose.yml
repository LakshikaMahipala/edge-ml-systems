version: "3.9"
services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: edge-ml-systems:dev
    volumes:
      - .:/workspace
      - ./data:/workspace/data
    working_dir: /workspace
    environment:
      - PYTHONPATH=/workspace/build_cpp
    tty: true
