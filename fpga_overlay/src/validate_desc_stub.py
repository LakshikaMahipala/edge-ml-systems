from __future__ import annotations
"""
Run later:
- validate JSON descriptors against schema (jsonschema package)
We keep as stub now to avoid dependency friction.
"""
def main():
    print("Run later: validate layer descriptors against JSON schema.")

if __name__ == "__main__":
    main()
